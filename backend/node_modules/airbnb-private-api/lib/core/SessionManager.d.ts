import { Session, SessioManagerConstructor } from '../types';
export declare class SessionManager {
    session_store: string;
    session_path: string;
    session: Session;
    email: string;
    phone: string;
    auth_token: string;
    currency: string;
    locale: string;
    authenticated: boolean;
    valid_store: boolean;
    constructor({ session_store, session_path, session, email, phone, auth_token, currency, locale, authenticated, valid_store, }: SessioManagerConstructor);
    _validate_store(): Promise<any>;
    _save_session(): Promise<unknown>;
    _load_session(): Promise<any>;
}
