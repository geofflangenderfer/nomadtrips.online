'use strict';
Object.defineProperty(exports, '__esModule', { value: true });
const constant_1 = require('../constant');
exports.random_id = (length, upper) => {
    let result = '';
    const characters = upper ? 'ABCDEFGHIJKLMNOPQRSTUVWXYZabcdefghijklmnopqrstuvwxyz0123456789' : 'abcdefghijklmnopqrstuvwxyz0123456789';
    const charactersLength = characters.length;
    for (let i = 0; i < length; i++) {
        result += characters.charAt(Math.floor(Math.random() * charactersLength));
    }
    return result;
};
exports.validate_user_type = type => !!~constant_1.user_types.indexOf(type);
exports.validate_reservation_order = type => !!~constant_1.reservation_order.indexOf(type);
